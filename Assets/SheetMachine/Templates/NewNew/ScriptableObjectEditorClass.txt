using UnityEngine;
using UnityEditor;
using UnityQuickSheet;

///
/// !!! Machine generated code !!!
///
[CustomEditor(typeof($ClassName))]
public class $ClassNameEditor : BaseGoogleEditor<$ClassName>
{	    
    public override bool Load()
    {        
        $ClassName targetData = target as $ClassName;
	
        var req = GoogleDataSettings.Instance.Service.Spreadsheets.Values.Get(targetData.SheetName, targetData.WorksheetName);
        var res = req.Execute();
        var rows = res.Values;
        targetData.dataArray = Serializer.Deserialize<$DataClassName>(rows, targetData.typeRowIndex);
        
        EditorUtility.SetDirty(targetData);
        AssetDatabase.SaveAssets();
        
        return true;
    }
}
